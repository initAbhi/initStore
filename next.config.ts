import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["files.stripe.com","lh3.googleusercontent.com","images.unsplash.com","plus.unsplash.com", "unsplash.com", "via.placeholder.com"]
  }   

};

export default nextConfig;
